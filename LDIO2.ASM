
;	LSX-Dodgers IO2
;	ÉÅÉÇÉäÇ™ãÛÇ¢ÇΩÇÁñﬂÇ∑

_SYS03:		;(BDOS)äOïîì¸óÕ
	DI
	PUSH	BC
SIOAD:	LD	BC,01F91H
	LD	A,5
	OUT	(C),A
	LD	A,0EAH		;RTS ON
	OUT	(C),A
REC1:
	CALL	BREAK2
	IN	A,(C)
	RRCA			;Arrival
	JR	NC,REC1
BREAK2X:
	CCF
	LD	A,5
	OUT	(C),A
	LD	A,0E8H		;RTS OFF
	OUT	(C),A

	DEC	C
	IN	A,(C)		;Read data
	POP	BC
	EI
	RET

BREAK2:
	LD	A,01AH
	IN	A,(1)
	RRCA
	RET	C
	POP	BC		;PC
SIOAD0:	LD	BC,01F91H
	JR	BREAK2X

_SYS04:		;(BDOS)äOïîèoóÕ
	PUSH	BC
SIOAD1:	LD	BC,01F91H
XMIT1:
	CALL	BREAK2
	LD	A,010H		;Reset status
	OUT	(C),A
	IN	A,(C)
	CPL
	AND	024H		;CTS Send Empty
	JR	NZ,XMIT1
	DEC	C
	OUT	(C),E
	POP	BC
	RET

